name: "Upload StarCoreLinkUtility"

on:
  push:
    branches:
      - main
    paths:
      - "Utility Mods/Stable/StarCoreLinkUtility/**"

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: AnarkisGaming/workshop@v1
        with:
          appID: 244850
          publishedFileID: 2927550954
          path: "Utility Mods/Stable/StarCoreLinkUtility"
          changelog: ${{ github.event.head_commit.message }}              
        env:
          STEAM_ACCOUNT_NAME: ${{ secrets.accountname }}
          STEAM_PASSWORD: ${{ secrets.password }}
